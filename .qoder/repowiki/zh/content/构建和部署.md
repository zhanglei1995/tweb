# 构建和部署

<cite>
**本文档中引用的文件**  
- [package.json](file://package.json)
- [vite.config.ts](file://vite.config.ts)
- [build.js](file://build.js)
- [server.js](file://server.js)
- [docker-compose.yaml](file://docker-compose.yaml)
- [.env](file://.env)
- [.env.local.example](file://.env.local.example)
- [keepAsset.js](file://keepAsset.js)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [生产构建流程](#生产构建流程)
4. [构建配置与优化](#构建配置与优化)
5. [部署选项](#部署选项)
6. [环境变量管理](#环境变量管理)
7. [性能监控与日志记录](#性能监控与日志记录)
8. [部署验证与回滚机制](#部署验证与回滚机制)

## 简介
本文档详细说明了tweb项目的构建和部署流程。文档涵盖了从代码压缩、资源优化到缓存策略的生产构建流程，解释了不同构建配置的用途和切换方法，描述了静态服务器部署和Docker容器化部署两种主要部署选项。同时提供了server.js和docker-compose.yaml的配置说明，涵盖了环境变量管理和多环境部署策略，并包含性能监控和错误日志记录的配置方法，最后提供了部署后的验证步骤和回滚机制。

## 项目结构
tweb项目采用模块化前端架构，主要包含源代码(src)、公共资源(public)、快照服务器(snapshot-server)和构建部署相关文件。项目使用Vite作为构建工具，SolidJS作为前端框架，通过pnpm进行包管理。

```mermaid
graph TD
A[tweb项目] --> B[src]
A --> C[public]
A --> D[snapshot-server]
A --> E[构建部署文件]
B --> B1[components]
B --> B2[config]
B --> B3[environment]
B --> B4[helpers]
B --> B5[lib]
B --> B6[pages]
B --> B7[scripts]
B --> B8[scss]
B --> B9[stores]
C --> C1[assets]
C --> C2[changelogs]
C --> C3[JS/CSS文件]
D --> D1[public]
D --> D2[index.js]
D --> D3[package.json]
E --> E1[package.json]
E --> E2[vite.config.ts]
E --> E3[build.js]
E --> E4[server.js]
E --> E5[docker-compose.yaml]
E --> E6[.env]
```

**Diagram sources**
- [package.json](file://package.json#L1-L76)
- [vite.config.ts](file://vite.config.ts#L1-L162)

**Section sources**
- [package.json](file://package.json#L1-L76)
- [vite.config.ts](file://vite.config.ts#L1-L162)

## 生产构建流程
tweb项目的生产构建流程通过一系列自动化脚本实现，确保代码压缩、资源优化和缓存策略的有效实施。

### 构建命令流程
生产构建通过`npm run build`命令触发，该命令执行一系列预定义的构建步骤：

```mermaid
sequenceDiagram
participant 开发者
participant package.json
participant build.js
participant Vite
开发者->>package.json : 执行 npm run build
package.json->>build.js : 调用 build.js 脚本
build.js->>build.js : 格式化语言文件
build.js->>build.js : 应用新语言包
build.js->>build.js : 更新版本号
build.js->>Vite : 触发 Vite 构建
Vite->>Vite : 编译 TypeScript
Vite->>Vite : 处理 SCSS
Vite->>Vite : 代码压缩
Vite->>Vite : 生成资源文件
Vite-->>build.js : 返回构建结果
build.js->>build.js : 复制文件到 public 目录
build.js->>build.js : 清理旧文件
build.js->>build.js : 上传到服务器(如有配置)
```

**Diagram sources**
- [package.json](file://package.json#L11)
- [build.js](file://build.js#L185-L208)
- [vite.config.ts](file://vite.config.ts#L121-L138)

**Section sources**
- [package.json](file://package.json#L11)
- [build.js](file://build.js#L1-L212)
- [vite.config.ts](file://vite.config.ts#L1-L162)

### 代码压缩与资源优化
tweb项目通过Vite构建工具实现代码压缩和资源优化，确保生产环境的性能最优。

```mermaid
flowchart TD
A[源代码] --> B[Vite构建]
B --> C{是否生产环境}
C --> |是| D[代码压缩]
C --> |否| E[开发模式]
D --> F[移除调试代码]
D --> G[Tree Shaking]
D --> H[代码分割]
D --> I[资源哈希]
F --> J[优化后的代码]
G --> J
H --> J
I --> J
J --> K[生产构建输出]
```

**Diagram sources**
- [vite.config.ts](file://vite.config.ts#L127)
- [build.js](file://build.js#L112-L116)

**Section sources**
- [vite.config.ts](file://vite.config.ts#L121-L138)
- [build.js](file://build.js#L112-L116)

### 缓存策略
tweb项目实施了多层次的缓存策略，以优化用户体验和减少服务器负载。

```mermaid
flowchart LR
A[客户端缓存] --> B[服务端缓存]
A --> C[CDN缓存]
A --> A1[浏览器缓存]
A1 --> A1a[Cache-Control: no-store]
A1 --> A1b[ETag禁用]
B --> B1[服务器压缩]
B1 --> B1a[使用compression中间件]
B1 --> B1b[HTTP压缩]
C --> C1[静态资源缓存]
C1 --> C1a[资源文件哈希]
C1 --> C1b[长期缓存]
```

**Diagram sources**
- [server.js](file://server.js#L16-L21)
- [vite.config.ts](file://vite.config.ts#L124)

**Section sources**
- [server.js](file://server.js#L1-L39)
- [vite.config.ts](file://vite.config.ts#L121-L138)

## 构建配置与优化
tweb项目的构建配置通过vite.config.ts文件进行管理，支持多种构建选项和优化策略。

### Vite构建配置
Vite构建配置文件(vite.config.ts)定义了项目的核心构建参数和插件配置。

```mermaid
classDiagram
class ViteConfig {
+plugins : Plugin[]
+server : ServerOptions
+build : BuildOptions
+test : TestOptions
+resolve : ResolveOptions
+css : CssOptions
}
class BuildOptions {
+target : string
+sourcemap : boolean
+assetsDir : string
+copyPublicDir : boolean
+emptyOutDir : boolean
+minify : boolean
+rollupOptions : RollupOptions
}
class ServerOptions {
+port : number
+sourcemapIgnoreList : function
}
ViteConfig --> BuildOptions : 包含
ViteConfig --> ServerOptions : 包含
```

**Diagram sources**
- [vite.config.ts](file://vite.config.ts#L70-L161)

**Section sources**
- [vite.config.ts](file://vite.config.ts#L1-L162)

### 构建插件配置
tweb项目使用了多个Vite插件来增强构建功能和开发体验。

```mermaid
flowchart TD
A[Vite插件] --> B[solidPlugin]
A --> C[handlebars]
A --> D[basicSsl]
A --> E[visualizer]
A --> F[checker]
B --> B1[支持SolidJS]
B --> B2[JSX转换]
C --> C1[模板引擎]
C --> C2[HTML生成]
D --> D1[SSL支持]
D --> D2[HTTPS开发]
E --> E1[构建分析]
E --> E2[可视化报告]
F --> F1[类型检查]
F --> F2[ESLint集成]
```

**Diagram sources**
- [vite.config.ts](file://vite.config.ts#L71-L91)

**Section sources**
- [vite.config.ts](file://vite.config.ts#L1-L162)

### 资源保留策略
tweb项目通过keepAsset.js文件定义了哪些资源文件在构建过程中需要保留。

```mermaid
flowchart TD
A[资源文件] --> B{是否需要保留?}
B --> |是| C[保留文件]
B --> |否| D[可能被清理]
C --> C1[XML文件]
C --> C2[版本文件]
C --> C3[assets目录]
C --> C4[changelogs目录]
C --> C5[Web Manifest文件]
C --> C6[WASM文件]
C --> C7[Worker文件]
C --> C8[Handlebars模板]
```

**Diagram sources**
- [keepAsset.js](file://keepAsset.js#L1-L13)

**Section sources**
- [keepAsset.js](file://keepAsset.js#L1-L14)
- [build.js](file://build.js#L43-L55)

## 部署选项
tweb项目支持多种部署选项，包括静态服务器部署和Docker容器化部署。

### 静态服务器部署
静态服务器部署通过server.js文件配置，使用Express框架提供静态文件服务。

```mermaid
sequenceDiagram
participant 客户端
participant Express服务器
participant 文件系统
客户端->>Express服务器 : HTTP请求
Express服务器->>Express服务器 : 设置Cache-Control : no-store
Express服务器->>Express服务器 : 启用compression中间件
Express服务器->>文件系统 : 读取静态文件
文件系统-->>Express服务器 : 返回文件内容
Express服务器-->>客户端 : 响应(压缩后)
Note over Express服务器,文件系统 : 使用compression中间件进行GZIP压缩
```

**Diagram sources**
- [server.js](file://server.js#L1-L39)
- [package.json](file://package.json#L10)

**Section sources**
- [server.js](file://server.js#L1-L39)
- [package.json](file://package.json#L10)

### Docker容器化部署
Docker容器化部署通过docker-compose.yaml文件配置，支持开发和生产环境的容器化部署。

```mermaid
graph TD
A[Docker Compose] --> B[tweb.dependencies]
A --> C[tweb.develop]
A --> D[tweb.production]
B --> B1[安装依赖]
B --> B2[挂载node_modules]
C --> C1[开发环境]
C --> C2[端口映射8080]
C --> C3[挂载整个项目]
C --> C4[启动开发服务器]
D --> D1[生产环境]
D --> D2[端口映射80]
D --> D3[构建生产镜像]
```

**Diagram sources**
- [docker-compose.yaml](file://docker-compose.yaml#L1-L30)

**Section sources**
- [docker-compose.yaml](file://docker-compose.yaml#L1-L30)

### Docker构建流程
Docker构建流程通过docker-compose.yaml文件定义的Dockerfile实现。

```mermaid
flowchart LR
A[构建上下文] --> B[Dockerfile]
B --> C[基础镜像]
C --> D[安装依赖]
D --> E[构建应用]
E --> F[暴露端口]
F --> G[启动命令]
subgraph tweb.dependencies
D1[安装pnpm依赖]
D2[挂载node_modules]
end
subgraph tweb.develop
E1[挂载源代码]
E2[启动开发服务器]
end
subgraph tweb.production
E3[构建生产版本]
E4[启动生产服务器]
end
```

**Diagram sources**
- [docker-compose.yaml](file://docker-compose.yaml#L5-L27)

**Section sources**
- [docker-compose.yaml](file://docker-compose.yaml#L1-L30)

## 环境变量管理
tweb项目通过环境变量文件实现多环境配置管理。

### 环境变量文件结构
项目使用多种环境变量文件来管理不同环境的配置。

```mermaid
graph TD
A[环境变量] --> B[.env]
A --> C[.env.local]
A --> D[.env.local.example]
B --> B1[默认环境变量]
B --> B2[生产环境配置]
C --> C1[本地覆盖配置]
C --> C2[开发环境特定]
D --> D1[本地配置模板]
D --> D2[示例文件]
```

**Diagram sources**
- [.env](file://.env#L1-L17)
- [.env.local.example](file://.env.local.example#L1-L2)

**Section sources**
- [.env](file://.env#L1-L17)
- [.env.local.example](file://.env.local.example#L1-L2)

### 环境变量配置
tweb项目的关键环境变量配置如下表所示：

| 环境变量 | 描述 | 示例值 |
|---------|------|-------|
| VITE_API_ID | Telegram API ID | 1025907 |
| VITE_API_HASH | Telegram API Hash | 452b0359b988148995f22ff0f4229750 |
| VITE_PUSH_SERVER_KEY | 推送服务器密钥 | BB3JzmQIBTl8ZdTmLzhkZVnoYxoSvUmCe1CqqF53u2j0WbEFo2m--mOBagroDFdqMeESfz3ue1Z1e5PTIxMYH_g |
| VITE_VERSION | 应用版本 | 2.2 |
| VITE_VERSION_FULL | 完整版本号 | 2.2 (611) |
| VITE_BUILD | 构建号 | 611 |
| VITE_LANG_PACK_VERSION | 语言包版本 | 240618 |
| VITE_MTPROTO_WORKER | MTPROTO Worker启用 | 1 |
| VITE_MTPROTO_AUTO | 自动选择MTPROTO协议 | 1 |
| VITE_MTPROTO_HAS_HTTP | 支持HTTP协议 | 1 |
| VITE_MTPROTO_HAS_WS | 支持WebSocket协议 | 1 |
| VITE_LANG_PACK_LOCAL_VERSION | 本地语言包版本 | 1 |

**Section sources**
- [.env](file://.env#L1-L17)
- [.env.local.example](file://.env.local.example#L1-L2)

### 多环境部署策略
tweb项目支持多环境部署，通过不同的构建和部署配置实现。

```mermaid
flowchart TD
A[部署环境] --> B[开发环境]
A --> C[测试环境]
A --> D[生产环境]
B --> B1[热重载]
B --> B2[详细日志]
B --> B3[源码映射]
C --> C1[模拟生产]
C --> C2[性能测试]
C --> C3[安全扫描]
D --> D1[代码压缩]
D --> D2[缓存优化]
D --> D3[错误监控]
subgraph 部署流程
E[代码提交] --> F[CI/CD流水线]
F --> G[构建验证]
G --> H[环境选择]
H --> I[部署执行]
I --> J[健康检查]
end
```

**Section sources**
- [package.json](file://package.json#L8-L11)
- [server.js](file://server.js#L9-L14)
- [docker-compose.yaml](file://docker-compose.yaml#L12-L30)

## 性能监控与日志记录
tweb项目通过多种机制实现性能监控和错误日志记录。

### 性能监控配置
项目通过Vite的visualizer插件实现构建性能分析。

```mermaid
flowchart LR
A[Vite构建] --> B[visualizer插件]
B --> C[生成构建报告]
C --> D[treemap视图]
D --> E[分析包大小]
E --> F[优化建议]
subgraph 监控指标
G[包大小]
H[依赖关系]
I[模块分布]
J[压缩效果]
end
```

**Diagram sources**
- [vite.config.ts](file://vite.config.ts#L87-L90)

**Section sources**
- [vite.config.ts](file://vite.config.ts#L87-L90)

### 错误日志记录
tweb项目通过多种方式实现错误日志记录和处理。

```mermaid
flowchart TD
A[错误发生] --> B{错误类型}
B --> |构建错误| C[build.js日志]
B --> |运行时错误| D[浏览器控制台]
B --> |服务器错误| E[Express错误处理]
C --> C1[编译错误]
C --> C2[依赖错误]
C --> C3[部署错误]
D --> D1[JavaScript错误]
D --> D2[网络错误]
D --> D3[资源加载错误]
E --> E1[HTTP错误]
E --> E2[文件读取错误]
E --> E3[端口占用错误]
```

**Section sources**
- [build.js](file://build.js#L199-L206)
- [server.js](file://server.js#L36-L38)

## 部署验证与回滚机制
tweb项目建立了完整的部署验证和回滚机制，确保部署的可靠性和可恢复性。

### 部署验证流程
部署后需要进行一系列验证步骤，确保应用正常运行。

```mermaid
flowchart TD
A[部署完成] --> B[健康检查]
B --> C[基本功能测试]
C --> D[性能基准测试]
D --> E[安全扫描]
E --> F[用户验收测试]
F --> G[监控告警设置]
G --> H[部署完成]
subgraph 验证指标
I[HTTP状态码]
J[响应时间]
K[资源加载]
L[功能完整性]
end
```

**Section sources**
- [server.js](file://server.js#L36-L38)
- [package.json](file://package.json#L10)

### 回滚机制
当部署出现问题时，tweb项目支持快速回滚到之前的稳定版本。

```mermaid
flowchart LR
A[发现问题] --> B[评估影响]
B --> C{是否需要回滚?}
C --> |是| D[停止当前部署]
C --> |否| E[修复问题]
D --> F[恢复备份]
F --> G[重启服务]
G --> H[验证回滚]
H --> I[通知团队]
subgraph 回滚策略
J[版本备份]
K[数据库快照]
L[配置备份]
M[自动化脚本]
end
```

**Section sources**
- [build.js](file://build.js#L117-L143)
- [server.js](file://server.js#L13-L14)